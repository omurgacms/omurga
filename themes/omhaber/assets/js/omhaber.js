document.addEventListener('click',function(e){const btn=e.target.closest('[data-omh-menu]');if(btn){const nav=document.querySelector('[data-omh-nav]');if(nav)nav.classList.toggle('is-open')}});document.querySelectorAll('[data-omh-slider]').forEach(function(slider){const slides=[...slider.querySelectorAll('.omh-hero-slide')];const nums=[...slider.querySelectorAll('[data-goto]')];let active=0;function go(i){active=i;slides.forEach((s,k)=>s.classList.toggle('is-active',k===i));nums.forEach((b,k)=>b.classList.toggle('is-active',k===i))}nums.forEach(b=>b.addEventListener('click',()=>go(parseInt(b.dataset.goto||'0',10))));if(slides.length>1){let t=setInterval(()=>go((active+1)%slides.length),5000);slider.addEventListener('mouseenter',()=>clearInterval(t));slider.addEventListener('mouseleave',()=>{t=setInterval(()=>go((active+1)%slides.length),5000)})}});
